import React, { FC } from "react";
import { Home } from "../../icons";

const Divider = ({}: {}) => {

  return (
    <div className="my-3 mx-2 border-b border-b-darkcharcoal">
    </div>
  );
}

export default Divider;